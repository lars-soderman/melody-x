'use client';

import { LocalProjectsList } from '@/components/LocalProjectsList';

export default function LocalProjectsPage() {
  return (
    <div className="p-6">
      <LocalProjectsList />
    </div>
  );
}
